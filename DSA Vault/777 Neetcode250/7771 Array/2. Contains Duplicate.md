# Contains Duplicate
[[Array]]
Given an integer array `nums`, return `true` if any value appears **more than once** in the array, otherwise return `false`.

**Example 1:**
```java
Input: nums = [1, 2, 3, 3]

Output: true
```

  
**Example 2:**
```java
Input: nums = [1, 2, 3, 4]

Output: false
```

**Solution:**
```java
class Solution {
    public boolean hasDuplicate(int[] nums) {
        boolean flag=false;
        for(int i=0;i<nums.length;i++){
            for(int j=i+1;j<nums.length;j++){
                if(nums[i]==nums[j]){
                    flag=true;
                }
            }
        }
        return flag;
    }

}
```

**Follow-Up Questions:**
[Valid Anagram - NeetCode](https://neetcode.io/problems/is-anagram/question)
https://neetcode.io/problems/two-integer-sum